# -------------------------------------------------------------------
# EarthFrame backend configuration
# Copy this file to `.env` and adjust values as needed.
# -------------------------------------------------------------------

# General application configuration
# -------------------------------------------------------------------
# "development" | "production"
ENV=development
# FastAPI server port
PORT=8000

# Frontend configuration
# -------------------------------------------------------------------
FRONTEND_ORIGIN=http://localhost:5173

# Database configuration
# -------------------------------------------------------------------
# For local dev (Postgres on host):
DATABASE_URL=postgresql+asyncpg://earthframe:earthframe@localhost:5432/earthframe

# For Docker Compose (Postgres service is "db"):
# DATABASE_URL=postgresql+asyncpg://earthframe:earthframe@db:5432/earthframe

# Test Database
# -------------------------------------------------------------------
# For local dev (Postgres on host):
TEST_DATABASE_URL=postgresql+asyncpg://earthframe:earthframe@localhost:5432/earthframe_test

# For Docker Compose (Postgres service is "db"):
# TEST_DATABASE_URL=postgresql+asyncpg://earthframe:earthframe@db:5432/earthframe_test

# Security configuration
# -------------------------------------------------------------------
JWT_SECRET_KEY=superlongrandomjwtkey
RESET_PASSWORD_SECRET_KEY=superlongrandomresetkey
VERIFICATION_SECRET_KEY=superlongrandomverifykey
